import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kiumyzu.familyfinance',
  appName: 'Family Finance App',
  webDir: 'out'
};

export default config;
